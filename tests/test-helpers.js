import { describe, it, beforeEach, afterEach, before, after } from 'node:test'
import assert from 'node:assert/strict'

const expect = assert

export { describe, it, beforeEach, afterEach, before, after, expect }
